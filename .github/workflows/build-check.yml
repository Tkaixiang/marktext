name: Build Check

on:
  push:
    branches: [ main, develop, 'claude/**' ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build-linux:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'
        cache: 'npm'

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          build-essential \
          libx11-dev \
          libxkbfile-dev \
          libsecret-1-dev \
          libgtk-3-dev

    - name: Install Node.js dependencies
      run: |
        npm install --ignore-scripts

    - name: Build application
      run: npm run build

    - name: Verify build output
      run: |
        test -f out/main/index.js || exit 1
        test -f out/preload/index.js || exit 1
        test -f out/renderer/index.html || exit 1
        echo "✅ Build successful"
        ls -lh out/main/index.js out/preload/index.js

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: build-output
        path: out/
        retention-days: 7

  # Optional: Add Windows and macOS builds when Electron download is resolved
  # build-windows:
  #   runs-on: windows-latest
  #   steps:
  #     # Similar steps for Windows

  # build-macos:
  #   runs-on: macos-latest
  #   steps:
  #     # Similar steps for macOS
